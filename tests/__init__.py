"""Offline unit tests for buyerlist's pure functions.

Making this a package (rather than a bare directory of test files) lets the
modules share the factories in `conftest.py` via a relative import.
"""
